# Session Workflow Rules

This project uses Session Workflow to manage AI coding sessions.

- Use agents defined in the `agents/` directory for SDD workflow.
- Refer to `.session/docs/agnostic-workflow-guide.md` for orchestration rules.
